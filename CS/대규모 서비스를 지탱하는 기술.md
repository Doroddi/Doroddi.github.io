# Chapter 2. 대규모 데이터 처리 입문
## 메모리와 디스크, 웹 애플리케이션과 부하

## 대규모 데이터 특유의 환경 알기

## 하테나 북마크의 데이터 규모
### 데이터가 많을수록 처리에 시간이 걸린다.

### 하테나 북마크를 예로 본 대규모 데이터
하테나 북마크의 어느 엔트리에 어떤 키워드가 포함되어 있는지를 나타내는 DB 테이블의 건수를 출력하면 3억 5천만 레코드가 존재함. 이 테이블에 갑자기 select * from relword와 같은 쿼리를 실행하면 응답이 반환되지 않는데, 그 이유는 3억 레코드 이상이 존재하기 때문.

### 하테나 북마크의 데이터 규모
위 경우 이외에도 레코드 건수가 1,500만이나 5,000만 정도는 됨. 데이터 크기로는 entry라는 테이블이 3GB, bookmark라는 테이블이 5.5GB, tag 테이블이 4.8GB, 그리고 HTML 텍스트 데이터도 압축해서 저장하고 있는데, 이것이 200GB를 넘음. relword라는 키워드용 테이블도 대략 10GB 정도. 전체적으로 간단히 말해 기가 바이트 단위임.  
Google이나 Yahoo!가 사용하고 있는 규모가 되면 테라바이트, 페타바이트가 되므로 이는 초대규모이며, 이에 비교하자면 하테나는 대규모~중규모 정도임.  
보통 웹 애플리케이션을 만들면서 기가바이트 단위의 DB는 좀처럼 나오지 않으므로 일반적인 웹 애플리케이션이라는 관점에서 보면 큰 규모임. 하테나 북마크는 일본에서 최대의 소셜 북마크 서비스로, 이 정도 데이터 규모의 웹 서비스로는 하테나 북마크가 사용자 수 관점에서도 큰 규모라고 할 수 있음. 

### 대규모 데이터로의 쿼리 – 데규모 데이터를 다루는 감각
레코드 수가 수천만 건 단위, 데이터 크기는 수 GB부터 수백 GB. 이 정도의 데이터가 되면 아무 생각 없이 던진 쿼리에 대해 응답하지 않음.  
데이터가 많으면 처리하는데 시간이 걸리게 됨.

## 대규모 데이터 처리의 어려운 점
### 메모리와 디스크

### 대규모 데이터는 어떤 점이 어려운가? - 메모리 내에서 계산할 수 없다.
제1포인트는 ‘메모리 내에서 계산할 수 없다’는 점. 메모리 내에서 계산할 수 없다는 점이 어려운 점인 이유는 메모리에 올리지 않으면 기본적으로 디스크를 계속 읽어가면서 검색하게 되어 좀처럼 발견할 수 없는 상태가 되기 때문. 데이터 건수가 많으면 그만큼 입력 데이터 건수가 늘어나므로 계산량이 많아진다는 점도 당연한 이유이지만, 이 점보다 문제가 되는 것은 ‘디스크를 읽고 있다’는 점.  
다시 말해 대규모 데이터의 어려운 점을 단적으로 말하면, ‘메모리 내에서 계산할 수 없다’는 것. 메모리 내에서 계산할 수 있다면, 아무리 무식한 방법으로 하더라도 그런대로 계산은 빨리 이뤄져서 200초나 기다릴 일은 없을 것임. 그러나 데이터가 너무 많으면 메모리 내에서 계산할 수 없으므로 디스크에 두고 특정 데이터를 검색하게 됨. 디스크는 메모리에 비해 상당히 느리므로 시간이 오래 걸림.

### 메모리와 디스크의 속도차 – 메모리는 10^5~10^6배 이상 고속
메모리 내의 특정 번지에 있는 데이터를 찾는 데이터 탐색과 디스크의 특정 원반 내에 있는 데이터를 찾는 것은 10^5~10^6 정도의 차이가 남. 10만~100만 배.

### 디스크는 왜 늦을까? - 메모리와 디스크
메모리는 전기적인 부품이므로 물리적 구조는 탐색속도와 그다지 관계없음.  
디스크는 동축 상에 ‘원반’(disk)이 쌓여 있음. 이 원반이 회전하고 있고 여기서 데이터를 읽어냄. 즉, 메모리와는 달리 회전 등의 물리적인 동작을 수반하고 있음. 이 물리적인 구조가 탐색속도에 영향을 줌.  
메모리는 1회 탐색할 때 마이크로초면 되지만, 디스크는 수 밀리초가 걸림.  
데이터가 메모리 상에 있다면 탐색할 때 물리적인 동작 없이 실제 데이터 탐색 시의 오버헤드가 거의 없으므로 빠른 것.  
탐색에 사용되는 것이 CPU의 캐시에 올리기 쉬운 알고리즘이나 데이터 구조라면 메모리 내용이 CPU 캐시에 올라가므로 더욱 빨리 처리할 수 있음(나노초 단위).

### OS 레벨에서의 연구
디스크는 느리지만 OS는 이것을 어느 정도 커버하는 작용을 함. OS는 연속된 데이터를 같은 위치에 쌓고 데이터를 읽을 때 1바이트씩 읽는 것이 아니라 4KB(kilobytes) 정도를 한꺼번에 읽도록 되어 있음.  
이렇게 함으로써 비슷한 데이터를 비슷한 곳에 두어 1번의 디스크 회전으로 읽는 데이터 수를 많게 하고, 그 결과 디스크의 회전 횟수를 최소화할 수 있게 됨. 그럼에도 결국 회전 1회당 밀리초 단위이므로 역시 메모리와의 속도 차를 피할 수 있는 것은 아님.

### 전송속도, 버스의 속도 차
메모리와 디스크는 모두 CPU와 버스로 연결되어 있음. 찾은 데이터를 디스크에서 메모리로 보내거나 메모리에서 CPU로 보내는 등 컴퓨터 내부에서 전송하기 위한 전송하기 위한 속도 차는 대략 100배 정도 차이남.  
메모리와 CPU는 상당히 빠른 버스로 연결되어 있음. 데이터가 많아지면 많아질수록 디스크와 메모리의 차이도 나타나게 되므로 전송속도에서도 디스크는 늦어짐.  
최근 나오고 있는 SSD(Solid State Drive)는 물리적인 회전이 아니므로 Seek(탐색)은 빠르지만 버스 속도가 병목이 되거나 그 밖의 구조에 기인하는 면이 있어서 역시 메모리만큼의 속도는 나오지 않음.

## 규모조정의 요소

### 규모조정, 확장성
대규모 환경이라고 하면 서버를 여러 대 나열해 놓고 그 서버로 부하를 분산함. 웹 서비스에서 자주 거론되는 규모조정(scaling), 확장성(scalability)이 해당 내용.  
웹 서비스에서는 고가의 빠른 하드웨어를 사서 성능을 높이는 ‘스케일업(scale-up)’ 전략보다도 저가이면서 일반적인 성능의 하드웨어를 많이 나열해서 시스템 전체 성능을 올리는 ‘스케일아웃(scale-out)’ 전략이 주류임. 스케일아웃 전략이 더 나은 이유는 웹 서비스에 적합한 형태이고 비용이 저렴하다는 점과 시스템 구성에 유연성이 있다는 점이 포인트.  
시스템 구성의 유연성이란, 예를 들어 부하가 적을 때는 최소한으로 투자하고 부하가 높아짐에 따라 확장해가기 쉽다는 것, 혹은 상당한 용도의 서버도 저렴하고 간단하게 준비할 수 있음. 여러모로 빠르게 상황대처가 쉬움.

### 규모조정의 요소 – CPU 부하와 I/O 부하
스케일아웃은 하드웨어를 나열해서 성능을 높이는, 즉, 하드웨어를 횡으로 전개해서 확장성을 확보해가게 됨. 이때 CPU 부하의 확장성을 확보하기는 쉬움.  
HTTP 요청을 받아 DB에 질의하고 DB로부터 응답받은 데이터를 가공해서 HTML로 클라이언트에 반환할 때는 기본적으로 CPU 부하만 소요되는 부분임. 이것은 서버 구성 중 프록시나 AP 서버(application server)가 담당할 일임.  
DB 서버 측면에서는 I/O 부하가 걸림.

### 웹 애플리케이션과 부하의 관계
웹 애플리케이션의 3단 구조에는 프록시, AP 서버, DB가 있음.  
기본적으로 AP 서버에는 I/O 부하가 걸리지 않고 DB 측에 I/O 부하가 걸림.  
AP 서버는 CPU 부하만 걸리므로 분산이 간단함. 그 이유는 기본적으로 데이터를 분산해서 갖고 있는 것이 아니므로 동일한 호스트가 동일하게 작업을 처리하기만 하면 분산할 수 있음. 따라서 대수를 늘리기만 하면 간단히 확장해갈 수 있음. 결국 새로운 서버를 추가하고자 한다면 원래 있던 서버와 완전히 동일한 구성을 갖는 서버, 심하게 말하면 복사본을 마련해서 추가하면 됨. 요청을 균등하게 분산하는 것은 로드밸런서(load balancer)라는 장치가 해줌.  
I/O 부하에는 문제가 있음. DB를 추가한다고 하면 예를 들어 쓰기가 발생했을 때 추가된 DB에 데이터를 어떻게 동기화할 것인가라는 문제가 생김. 쓰기는 간단히 분산할 수가 없음.

### DB 확장성 확보의 어려움
대규모 환경에서는 I/O 부하를 부담하고 있는 서버는 애초에 분산시키기 어려운데다가 디스크 I/O가 많이 발생하면 서버가 금새 느려지는 본질적인 문제가 있음.  
서비스가 무거울 때 서버를 늘려서 해결할 수 있다면 간단한 일임. 하지만 그렇게 해결되지 않기 때문에 어려운 것.

## 대규모 데이터를 다루기 위한 기초지식
1. 프로그램을 작성할 때의 요령
2. 프로그램 개발의 근간이 되는 기초라는 점에서 전제로서 알아두었으면 하는 것

### 대규모 데이터를 다루는 세 가지 급소 – 프로그램을 작성할 때의 요령
- 어떻게 하면 메모리에서 처리를 마칠 수 있을까?  
메모리에서 처리를 마쳐야 하는 이유는 디스크 seek 횟수가 확장성, 성능에 크게 영향을 주기 때문에 디스크 seek 횟수를 최소화한다는 의미로 메모리를 활용. 국소성을 활용하 분산.  

- 데이터량 증가에 강한 알고리즘 사용  
알고리즘, 자료구조를 통해 탐색 시간 줄이기

- 데이터 압축이나 검색기술과 같은 테크닉 활용  
압축해서 데이터량을 줄일 수 있다면 읽어내는 seek 횟수도 적어지게 되므로 디스크 읽는 횟수를 최소화할 수 있음. 또한 메모리에 캐싱하기 쉬워짐. 데이터가 크면 메모리에서 넘치거나 디스크에 저장해도 읽어내기에 시간이 걸리므로 압축이 중요.  
검색이 중요한 이유는 확장성 면에서 DB에만 맡겨서 해결할 수 없을 때, 특정 용도에 특화된 검색엔진 등을 만들어서 해당 검색 시스템을 웹 애플리케이션에서 이용하는 형태로 전환한다면 속도를 제대로 확보할 수 있기 때문.

### 대규모 데이터를 다루기 전 3대 전제지식 – 프로그램 개발의 한층 아래 기초
1. OS 캐시 2. 분산을 고려한 RDBMS 운용 3. 알고리즘과 데이터 구조

# CHAPTER 3. OS 캐시와 분산
## 대규모 데이터를 효율적으로 처리하는 원리

## 대규모 데이터를 다룰 때의 포인트
### I/O 대책에 대한 기반은 OS에 있다.

## OS의 캐시구조

### OS의 캐시 구조를 알고 애플리케이션 작성하기 – 페이지 캐시
OS에는 디스크 내의 데이터에 빠르게 액세스할 수 있도록 하는 구조가 갖춰져 있음. OS는 메모리를 이용해서 디스크 액세스를 줄임.  
Linux의 경우 페이지 캐시(page cache), 버퍼 캐시(buffer cache)라고 하는 캐시 구조를 갖추고 있음.  
OS는 ‘가상 메모리 구조’를 갖추고 있음. 가상 메모리 구조는 논리적인 선형 어드레스를 물리 어드레스로 변환하는 것.

### 가상 메모리 구조
가상 메모리 구조가 가장 큰 이유는 물리적인 하드웨어를 OS에서 추상화하기 위함.  
OS는 메모리를 직접 프로세스로 넘기는 것이 아니라 일단 커널 내에서 메모리를 추상화하고 있음. 이것이 가상 메모리 구조.  
디스크의 경우에도 OS가 모아서 읽어낸다고 했는데, 메모리를 확보할 때에도 그와 마찬가지 방식으로 메모리 1바이트씩 액세스하는 것이 아니라 적당히 4KB 정도를 블록으로 확보해서 프로세스에 넘김. 여기서 1개의 블록을 ‘페이지’라고 함. OS는 프로세스에서 메모리를 요청받으면 페이지를 1개 이상, 필요한 만큼 페이지를 확보해서 프로세스에 넘기는 작업을 수행함.

### Linux의 페이지 캐시 원리
OS는 확보한 페이지를 메모리상에 계속 확보해두는 기능을 갖고 있음.  
OS는 디스크로부터 4KB 크기의 블록을 읽어냄. 읽어낸 데이터는 한 번은 메모리상에 위치시켜야 프로세스가 액세스 할 수 있음(가상 메모리). 그리고 OS는 그 메모리 주소를 프로세스에 가상 어드레스로서 알려줌. 그러면 프로세스는 해당 메모리에 액세스하게 됨.  
데이터 읽기를 마친 프로세스는 해당 데이터가 불필요하게 됐어도 메모리에 남겨 두고 그렇게 하면 다음에 다른 프로세스가 같은 데이터를 디스크에서 읽어야할 때 남겨두었던 페이지를 사용할 수 있으므로 불필요한 디스크 읽기를 피할 수 있음. 이것이 페이지 캐시. 즉, 커널이 한 번 할당한 메모리를 해제하지 않고 계속 남겨두는 것이 페이지 캐시의 기본.  
이는 예외인 경우를 제외하고 모든 I/O에 투과적으로 작용함. Linux에서는 디스크에 데이터를 읽으러 가면 꼭 한 번은 메모리로 가서 데이터가 반드시 캐싱됨. 이로인해 두 번째 액세스가 빨라짐. 현대의 OS는 대체로 페이지 캐시와 비슷한 구조를 갖추고 있음. OS를 계속 가동 시켜 두면 메모리가 허락하는 한 디스크 상의 데이터를 계속 캐싱함. 따라서 OS를 계속 켜두면 빨라짐.  

### VFS
디스크의 캐시는 페이지 캐시에 의해 제공되지만, 실제 이 디스크를 조작하는 디바이스 드라이버와 OS 사이에는 파일 시스템이 끼어 있음. 파일 시스템 하위에는 디바이스 드라이버가 있으며, 디바이스 드라이버가 실제로 하드디스크 등을 조작. 파일시스템 위에는 VFS(Virtual File System, 가상 파일시스템)이라는 추상화 레이어가 있음. 파일시스템은 다양한 함수를 갖추고 있는데, 그 인터페이스를 통일하는 것이 VFS의 역할. 또한 VFS가 페이지 캐시의 구조를 지니고 있음.  VFS의 역할은 파일 시스템 구현의 추상화와 성능에 관련된 페이지 캐시 부분.

###Linux는 페이지 단위로 디스크를 캐싱한다
OS는 파일을 읽어낸 블록 단위만으로 캐싱할 수 있는 범위가 정해짐. 디스크상에 배치되어 있는 특정파일의 일부분만, 읽어낸 부분만 캐싱 가능. 이렇게 디스크를 캐싱하는 단위가 페이지.  구조상으로는 LRU(Least Recently Used), 가장 오래된 캐싱된 페이지를 파기하고 가장 새로운 것을 남겨놓는 형태로 되어 있으므로 최근에 읽은 부분이 캐시에 남고 과거에 읽은 부분은 파기됨. 따라서 DB도 계속 구동시키면 캐시가 점점 최적화되어 가므로 기동시킨 직후보다 점점 뒤로 갈수록 부하, I/O가 내려가는 특성을 보임.  
Linux는 파일을 i노드 번호라고 하는 번호로 식별하며, 해당 파일의 i노드 번호와 해당 파일의 어느 위치부터 시작할지를 나타내는 오프셋, 이 두 가지 값을 키로 캐싱함. 이 두 가지를 키로 하면 ‘어떤 파일의 어느 위치를’이라는 쌍으로 캐시의 키를 관리할 수 있으므로 결과적으로 파일 전체가 아닌 파일의 일부를 캐싱해갈 수 있음.  
그리고 파일이 아무리 크더라도 이 키로부터 해당 페이지를 찾을 때의 데이터 구조는 최적화되어 있음. OS(=커널) 내부에서 사용되고 있는 데이터 구조는 Radix Tree라고 하며, 파일이 아무리 커지더라도 캐시 탐색속도가 떨어지지 않도록 개발된 데이터 구조임. 따라서 커다란 파일의 일부분을 캐싱하거나 작은 파일의 일부분을 캐싱하더라도 동일한 속도로 캐시를 찾을 수 있도록 되어 있음.

### 메모리가 비어 잇으면 캐싱
Linux는 메모리가 비어 있으면 전부 캐싱함. 프로세스에서 메모리를 요청했을 때 캐시로 인해 더 이상 메모리가 남아있지 않다면 오래된 캐시를 버리고 프로세스에 메모리를 확보해줌.  

### 메모리를 늘려서 I/O부하 줄이기
메모리를 늘리면 캐시에 사용할 수 있는 용량이 늘어나고, 캐시에 사용할 수 있는 용량이 늘어나면 보다 많은 데이터를 캐싱할 수 있고, 많이 캐싱되면 디스크를 읽는 횟수가 줄어듦.  

### 페이지 캐시는 투과적으로 작용한다
특정 파일을 read하면 이를 쭉 캐싱해나감.

## I/O 부하를 줄이는 방법

### 캐시를 전제로 한 I/O 줄이는 방법
첫 번째 포인트는 데이터 규모에 비해 물리 메모리가 크면 전부 캐싱할 수 있음. 다루고자 하는 데이터의 크기에 주목.  
또한 대규모 데이터 처리에는 데이터 압축이 중요한데, 압축해서 저장해두면 디스크의 내용을 전부 그대로 캐싱해둘 수 있는 경우가 많음.  
또 하나는 경제적인 비용과의 밸런스를 고려하고자 한다는 점.

### 복수 서버로 확장시키기 – 캐시로 해결될 수 없는 규모일 경우
메모리를 늘려서 전부 캐싱할 수 있다면 좋지만, 데이터를 전부 캐싱할 수 없는 규모가 될 수 있음. 이때 복수 서버로 확장시키는 방안을 생각해볼 필요가 있음.  
AP 서버를 늘려야 하는 이유는 기본적으로 CPU 부하를 낮추고 분산시키기 위함. 하지만 DB 서버를 늘려야 할 때는 반드시 부하 때문만은 아니고 오히려 캐시 용량을 늘리고자 할 때 혹은 효율을 높이고자 할 때인 경우가 많음.  
따라서 AP 서버를 늘리는 것과 DB 서버를 늘리는 것은 둘 다 서버를 늘리는 것이지만 필요한 리소스, 요구되는 리소스가 전혀 다름.

### 단순히 대수만 늘려서는 확장성을 확보할 수 없다
서버를 늘림으로써 시스템 전체로서는 아주 조금은 빨라질지도 모르지만, 증설비용에 대비해서 성능향상은 극히 부족함.

## 국소성을 살리는 분산

### 국소성을 고려한 분산이란?
데이터에 대한 액세스 패턴을 고려해서 분산시키는 것을 국소성을 고려한 분산이라고 함.  
액세스 패턴을 고려해서 분배한 경우에는 더 이상 액세스되지 않는 캐시영역을 다른 곳으로 돌릴 수 있음. 결국 시스템 전체로서는 메모리에 올라간 데이터량이 늘어나게 됨.

### 파티셔닝 – 국소성을 고려한 분산 1
파티셔닝(partitioning)은 한 대였던 DB 서버를 여러 대의 서버로 분할하는 방법을 말함. 간단한 것은 ‘테이블 단위 분할’.  
테이블 단위로 분할했으면 요청도 각 서버로 보내 처리될 수 있도록 당연히 애플리케이션을 변경할 필요가 있음.  
다른 분할 방법으로는 ‘테이블 데이터 분할’이 있음. 특정 테이블 하나를 여러 개의 작은 테이블로 분할함.  
이 분할의 문제점은 분할의 입도를 크거나 작게 조절할 때 데이터를 한 번 병합해야 한다는 번거로움이 있다는 점.

### 요청 패턴을 ‘섬’으로 분할 – 국소성을 고려한 분산 2
‘용도별로 시스템을 섬으로 나누는 방법’도 있음.  
하테나 북마크에서는 HTTP 요청의 User-Agent나 URL 등을 보고, 통상의 사용자, 일부 API요청, Google bot이나 Yahoo! 등의 봇(bot) 등에 따라 섬으로 나누는 방법을 사용하고 있음.  
검색 봇은 그 특성상 아주 오래된 웹 페이지에도 액세스하러 옴. 사람의 경우라면 좀처럼 액세스하지 않을 페이지에도 액세스하고, 또한 광범위하게 액세스함. 그렇게 되면 캐시가 작용하기 어려움. 동일한 페이지에 잇따라 방문하는 경우에는 캐시로 성능을 끌어올리기 쉽지만, 광범위한 액세스에는 그럴 수가 없음.  
그러나 봇에 대해 그렇게 빨리 응답할 필요는 없으므로 섬으로 나눠놓음.  
한편, 봇 이외의 액세스, 즉 사용자로부터의 액세스는 최상위 페이지나 인기 엔트리 페이지 등 최신, 인기 페이지에 거의 액세스가 집중되므로 빈번하게 참조되는 부분은 캐싱하기 쉬움.  
이렇듯 캐싱하기 쉬운 요청, 캐싱하기 어려운 요청을 처리하는 섬을 나누게 되면, 전자는 국소성으로 인해 안정되고 높은 캐시 적중률을 낼 수 있게 됨. 후자의 요청이 전자의 캐시를 어지럽히므로 섬으로 나누는 경우에 비해 전체적으로는 캐시 효율이 떨어짐.

### 페이지 캐시를 고려한 운용의 기본 규칙
첫 번째 포인트는 OS 기동 직후에 서버를 투입하지 않는다는 것. 캐시가 쌓여있지 않기 때문.  
다음은 성능평가나 부하시험에 대한 것. 초깃값을 버려야 한다는 것을 기억. 성능평가나 부하시험을 할 땐 캐시가 최적화된 후에 실시할 필요가 있음.

# CAHPTER 4. DB 스케일아웃 전략
## 분산을 고려한 MySQL 운용

## 인덱스를 올바르게 운용하기
### 분산을 고려한 MySQL 운용의 대전제

### 분산을 고려한 MySQL 운용, 세 가지 포인트
1. OS 캐시 활용
2. 인덱스
3. 확장을 전제로 한 설계

### OS 캐시 활용
전체 데이터 크기에 주의해서 데이터량이 물리 메모리보다 가능한 한 적어지도록 유지. 메모리가 부족할 경우에는 증설.  
서비스를 설계하는 초기단계부터 그렇게까지 깊게 생각할 필요는 없지만, 어느 정도 규모가 있는 서비스가 되면 칼럼 변경, 스키마 변경에도 그에 상응하는 주의를 기울여야만 함.  
대량의 데이터를 저장하려는 테이블은 레코드가 가능한 한 적어지도록 컴팩트하게 설계하기.  
정규화하여 DB를 나누어 놓을 수도 있지만, 경우에 따라서는 쿼리가 복잡해져서 속도가 떨어지는 경우가 있으므로 속도와 데이터 크기 간 상반관계(trade-off)와 같은 부분도 생각해야 함.

### 인덱스의 중요성 – B트리
아고리즘, 데이터 구조에서 탐색을 할 때는 기본적으로 트리(탐색트리)가 널리 사용됨. 인덱스는 주로 탐색 시간을 줄이기 위함으로, 그 내부 구조로는 트리가 사용됨.  
MySQL의 인덱스는 기본적으로 B+트리. B트리는 데이터 삽입이나 삭제를 반복한 경우에도 트리의 형태에 치우침이 생기지 않는 ‘평형 트리’. 하드디스크 상에 구축하기에 알맞은 데이터 구조이므로 DB에서 자주 사용됨.  
루트에서 시작해서 각 노드에 찾고 있는 값이 저장되어 있는지 확인하고 없으면 자식을 찾아감. 이때 찾는 값의 대소관계로 어떤 자식을 찾아가면 될지가 한 번에 결정될 수 있는 규칙이 사용됨. 이에 따라 검색을 할 때 최대 트리 높이만큼의 횟수만 자식을 찾아가면 되므로 탐색이 빨라짐.  
이분 트리는 노드가 반드시 하나 또는 두 개로 정해져 있지만, B트리는 m=5처럼 개수가 정해짐. B트리는 이 수를 조정함으로써 블록의 크기를 4KB 등으로 나눌 수 있음. 즉, 각 노드의 크기를 적당한 사이즈로 정할 수 있음.  
노드의 크기라는 것은 디스크의 페이지와 매우 밀접한 관계가 있는데 노드 1개로 디스크의 1블록만큼을 할당하면, B트리로 디스크 상에 저장했을 때 각 노드를 딱 1블록만큼으로 저장할 수 있음.  
트리에서 검색을 할 때는 노드에서 노드로 트리를 순회함. B트리의 경우, 각 노드를 1블록에 모아서 저장되도록 구성할 수 있으므로 디스크 Seek 발생횟수를 노드를 찾아갈 때만으로 최소화할 수 있음. OS가 한 번에 읽어내서 메모리에 캐싱하게 되므로 같은 노드 내의 데이터는 디스크 Seek 없이 탐색할 수 있음.  
이분트리는 특정 노드를 모아서 1블록에 저장하는 등의 작업이 어려움. 그러므로 이분트리를 디스크 상에 저장하는데 있어서 디스크 구조에 최적화할 수가 없음. 그 결과, 디스크 상의 이분트리를 검색하려고 하면 여기저기 블록에 분산되어 있는 데이터를 읽어야 하므로 디스크 Seek 횟수가 많아지게 됨.  
MySQL에서 인덱스를 만들면 B+트리에 의한 트리 데이터 구조가 생김. 탐색에서는 처음부터 실 데이터를 살펴가는 것보다 인덱스의 트리를 순회하는 편이 더 빠름.

### 인덱스의 효과
계산량 측면에서 개선될 뿐만 아니라 디스크 구조에 최적화된 인덱스를 사용해서 탐색함으로써 디스크 Seek 횟수면에서도 개선됨.  
대규모가 되면 될수록 인덱스를 준비해놓느냐 아니냐에 따라 차이가 나게 됨. 크기가 커지면 인덱스 없이는 시작부터 액세스할 수 없는 상황이 될 수 있음. MySQL은 레코드 총 건수를 보고 인덱스를 사용하지 않는 편이 더 빠르다라고 판단되면, 사용하지 않는 최적화 작업을 내부에서 어느 정도 수행해줌.  

- 인덱스의 작용 – MySQL의 특성  
MySQL의 인덱스 사양에는 약간의 특성이 있는데, 걸어놓고 있는 칼럼을 대상으로 한 쿼리라도 던지는 SQL에 따라서는 그것이 사용되거나 사용되지 않기도 함.
기본적으로 where, order by, group by의 조건에 지정된 칼럼은 기본적으로 인덱스가 사용됨.  
MySQL은 alter table 명령 등으로 명시적으로 인덱스를 추가한 경우 이외에도 Primary Key나 UNIQUE 제약을 건 칼럼에도 인덱스를 가지고 있음. show index 명령으로 인덱스의 내용을 확인할 수 있음.  
MySQL은 한 번의 쿼리에서 하나의 인덱스만 사용한다는 특성을 갖고 있음. 여러 개의 인덱스를 태우고자 할 경우에는 복합 인덱스를 설정해 둘 필요가 있음.  

### 인덱스가 작용하는지 확인하는 법 – explain 명령
SQL을 던지기 전에 explain 명령을 실행하면 MySQL이 인덱스가 작용하는지 여부를 전부 조사해줌.  
- explain 명령에서 속도에 유의하라  
SQL을 수반하는 프로그램을 개발할 때 속도에 신경 쓰고자 할 때는 explain 명령으로 자신이 던지려고 하는 SQL에 제대로 인덱스가 작용하는지 여부를 확인하면서 개발하면 좋음.  
Extra 열에는 Using where 이외에 Using filesort나 Using temporary와 같은 항목이 나올 경우가 있음. 각각 레코드 정렬에 외부 정렬(외부 파일을 사용한 정렬)이나 임시 테이블이 필요하다는 의미. 기본적으로 Using filesort나 Using temporary가 나오는 것은 그다지 틀이 좋은 쿼리라고 할 수 없으므로 가능한 한 나오지 않도록 쿼리나 인덱스를 튜닝해갈 필요가 있음.

## MySQL의 분산
### 확장을 전제로 한 시스템 설계

### MySQL의 레플리케이션 기능
MySQL에는 기본 기능으로 레플리케이션(replication) 기능이 있음. 레플리케이션이란 마스터(master)를 정하고 마스터를 뒤따르는 서버(슬레이브, slave)를 정해두면, 마스터에 쓴 내용을 슬레이브가 폴링(polling)해서 동일한 내용으로 자신을 갱신하는 기능임. 슬레이브는 마스터의 레플리카(replica)가 되는 것. 이렇게 해서 동일한 내용의 서버를 여러 대 마련할 수 있음.  
마스터/슬레이브로 레플리케이션해서 서버를 여러 대 준비하게 되면, AP 서버에서는 로드밸러서를 경유해서 슬레이브로 질의함. 이렇게 해서 쿼리를 여러 서버로 분산할 수 있음.  
애플리케이션 구현에서 select 등 참조 쿼리만 로드밸런서로 흘러가도록 하고 갱신 쿼리는 마스터로 직접 던짐. 갱신 쿼리를 슬레이브로 던지게 되면 슬레이브와 마스터 간 내용을 동기화할 수 없음. MySQL은 마스터와 슬레이브 간 내용의 불일치를 감지하면 레플리케이션을 중지해버림.  
슬레이브 앞단에 로드밸런서를 사용하고 있는데, 로드밸런서를 사용하지 않는 방법도 있음. 애플리케이션 측에서 분배까지 제어하거나 MySQL Proxy와 같은 것을 사용하는 것.

### 마스터/슬레이브의 특징 – 참조계열은 확장하고 갱신계열은 확장하지 않는다
참조계열 쿼리는 확장을 위해 서버를 늘리면 되는데, 서버를 늘린다고는 해도 대수를 늘리기보다 메모리에 맞추는 것이 중요.  
마스터는 확장할 수 없음. 반드시 할 수 없는 것은 아니지만, 갱신계열 쿼리가 늘어나면 상당히 힘들어짐. 그렇지만 웹 애플리케이션에서는 대략 90% 이상이 참조계열 쿼리임. 쓰기는 상대적으로 훨씬 적기 때문에 참조계열에 비하면 마스터가 병목이 되어 곤란한 상황일 발생하는 경우는 그렇게 많지 않음.  
- 갱신/쓰기계열을 확장하고자 할 때 – 테이블 분할, key-value 스토어
드물지만 마스터에 엄청난 쓰기작업이 발생하는 애플리케이션을 개발하는 경우가 있음.  
이런 경우에도 테이블을 분할해서 테이블 크기를 작게 해줌. 그러면 분할로 인해 쓰기작업이 분산되고 테이블 파일이 분할되면 동일 호스트 내에서 여러 디스크를 가지고 분산시킬 수도 있으며, 서로 다른 서버로 분산할 수도 있음.  
처음부터 RDBMS를 사용하지 않는 방법도 생각해볼 수 있음. 단순히 값을 저장하고 RJsof 뿐이라면 RDB가 갖는 복잡한 통계처리나 범용적인 정렬처리 대신에, key-value 스토어가 오버헤드도 적고 압도적으로 빠르며 확장하기 쉬움.
그리고 애플리케이션 측면에서 처음부터 쓰기작업 횟수를 줄이는 것은 당연히 해야 함.

## MySQL의 스케일아웃과 파티셔닝

### MySQL의 스케일아웃 전략
데이터가 메모리에 올라가는 크기이면 메모리에 올리고, 올라가지 않으면 메모리를 증설, 그리고 인덱스를 제대로 걸자.  
메모리 증설이 불가능하다면 파티셔닝을 하자.

### 파티셔닝(테이블 분할)에 관한 보충
파티셔닝(테이블 분할)이란 테이블 A와 테이블 B를 서로 다른 서버에 놓아서 분산하는 방법. 파티셔닝은 국소성을 활용해서 분산할 수 있으므로 캐시가 유효하고 파티셔닝이 효과적임.

### 파티셔닝을 전제로 한 설계
파티셔닝에 관한 것 중 중요한 것은 파티셔닝을 전제로 한 설계임. 기본적으로 JOIN 쿼리는 대상이 되는 테이블을 앞으로도 서버 분할하지 않을 것이라고 보장할 수 있을 때만 사용.  
긴밀한 관계를 가지고 있는 테이블은 같은 호스트에 두고 그렇지 않은 테이블은 분할하는 방향처럼 애플리케이션 설계를 해야함.

### JOIN 배제 – where ... in ... 이용
INNER JOIN하는 쿼리를 where ... in ...을 이용해 동일한 데이터를 얻을 수 있음.  

### 파티셔닝의 상반관계
파티셔닝으로 분산할 수는 있지만, 사실 파티셔닝에는 상반관계가 존재함.  
파티셔닝의 좋은 점은 부하가 내려가고 국소성이 늘어나서 캐시 효과가 높아진다는 점.
- 운용이 복잡해진다  
용도가 다른 서버가 생기므로 각 서버가 무슨 일을 하는지 파악하는 것이 운용적으로 쉽지 않을 수 있음. 장애가 발생했을 때나 혼란한 상황에 빠졌을 때 민첩하게 대응하기 어려울 수 있음. 이는 곧 복구 시간 증가에 영향을 끼침.  
- 고장률이 높아진다  
대수가 늘어나는 만큼 고장확률이 높아지는 문제도 있음. 내장애성을 고려할 때 분할과 동반해서 머신을 늘릴 때는 1대만 늘려서는 끝나지 않음.
- 다중화에 필요한 서버 대수는 몇 대?  
서버가 1대 밖에 없다면 고장나면 끝임. 만약 마스터가 1대 있고 슬레이브가 3대로 다중화했다면 슬레이브가 1대 고장 나더라도 괜찮을 뿐 아니라 마스터가 고장 나더라도 슬레이브가 살아있으므로 슬레이브 중 하나를 마스터로 해주면 됨.  
슬레이브 1대가 고장나면 나머지 1대를 새로운 DB 서버에 복사해야하므로 반드시 남아있던 슬레이브를 중지해야 함. 마스터를 중지하면 쓰기작업을 할 수 없게 되고 슬레이브를 중지하면 참조할 수 없게 되므로 4대를 1세트로 씀. 무정지 복구를 위함.  
- 애플리케이션의 용도와 서버 대수  
무정지가 필수 조건인지 여부는 해당 애플리케이션의 용도에도 의존하므로 반드시 4대가 필요하다고는 할 수 없음.  
- 서버 대수와 고장률  
- 분할하면 대수가 한꺼번에 늘어나게 됨. 그렇게 되면 고장 확률도 올라가고 여기저기서 고장이 발생함.

# CHAPTER 5. 대규모 데이터 처리 실전 입문
## 애플리케이션 개발의 급소

## 용도특화형 인덱싱
### 대규모 데이터를 능수능란하게 다루기

### 인덱스와 시스템 구성 – RDBMS의 한계가 보일 때
전문 검색이나 유사문서계열 탐색, 데이터마이닝과 같은 작업에서는 RDBMS로는 한계가 있음.  
배치 처리로 RDBMS에서 데이터를 추출해서 별도의 인덱스 서버와 같은 것을 만들고, 이 인덱스 서버에 웹 애플리케이션에서 RPC(Remote Procedure Call)등으로 액세스하는 방법을 사용.  
- RPC, 웹 API  
Remote Procedure Call을 의미하는 RPC는 요즘은 웹 API라고 불림. 인덱스를 갖고 있는 웹 서버를 실행하고, JSON을 반환하는 애플리케이션을 만들어 웹 API로 JSON에 액세스함.  
AP 서버에 인덱스를 직접 저장하지 않는 이유는 AP 서버에 충분한 메모리가 탑재되어 있지 않은 경우가 많기 때문. 또한 AP 서버의 아키텍처 면에서 볼 때 커다란 검색 인덱스를 여러 프로세스에서 같이 사용하도록 구성하는 것은 적합하지 않음. 그래서 인덱스 전용 서버에서 처리하도록 하는 것.  
최근에는 JSON + HTTP, 즉 웹 API로 처리하는 일이 더 많음.

### 용도특화형 인덱싱 – 튜닝한 데이터 구조 사용하기
위와 같은 방법을 ‘용도특화형 인덱싱’이라고 함. 용도특화형 인덱싱을 하면 RDBMS에서는 어려운 일이 가능해짐. RDBMS라는 것은 데이터를 정렬하거나, 통계처리가 가능하거나, JOIN하는 등 범용적인 용도로 만들어져 있음. 즉, 다양한 목적에 사용할 수 있도록 만들어져 잇음. 뭔가 특정한 목적으로만 사용하고자 할 때에는 특정한 목적만으로 사용할 수 있도록 튜닝한 데이터 구조를 사용하는 것이 압도적으로 빠름. 검색에서 역 인덱스가 그 전형적인 예로, 자연언어처리와 같은 처리를 미리 한 다음 인덱스를 만들어두면 RDBMS로 데이터를 전부 순회하지 않아도 순식간에 검색할 수가 있음.  
- 키워드 링크  
Trie 기반의 정규표현을 사용해서 Common Prefix Search(공통접두사검색)을 수행. Trie와 Common Prefix Search 조합은 최고. DB에서 사전에 추출해서 만들어두고 매칭하는 것이 키워드 링크의 방법.  
- 전문 검색엔진  
대량의 데이터에서 검색하고자 함. 빠르게 검색하고자 함. ‘좋은 느낌(Feeling Lucky)’과 같은 문서를 상위로. 세 번째를 위해서 ‘스코어링’ 처리를 수행. 스코어링에서는 검색대상인 문서가 가지고 있는 다양한 정보를 복합적으로 이용. 이것도 특정 칼럼으로만 정렬할 수 있는 RDBMS에서는 무리. 반대로 검색 인덱스를 직접 만들면, 즉 전문 검색엔진을 직접 구현한다면 스코어링 알고리즘도 자유롭게 선택할 수 있으므로 검색결과를 나열하는 방법은 RDBMS를 사용하는 것보다 훨씬 유연하게 구성할 수 있음.

## 이론과 실전 양쪽과의 싸움

### 요구되는 기술적 요건 규명하기
- 대규모 웹 애플리케이션에 있어서 이론과 실전  
대규모 웹 애플리케이션을 개발, 운용하려고 하면 이론과 실전 모두를 하지 않으면 안됨. 이론과 실전 사이에서 균형을 잘 맞춰 실행해가는 것이 회사에 요구되는 기술적 요건.  
- 컴퓨터의 문제에 이르는 길을 어떻게 발견할까?  
알고리즘/데이터 구조로 무언가를 실행할 때 해당 알고리즘의 데이터 구조를 사용해서 어떤 게 가능한지를 어느 정도 머릿속에 넣어두는 것이 좋음. 이론 + 응용을 위한 이치를 익혀두는 것이 중요.
